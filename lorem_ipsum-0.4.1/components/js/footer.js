const footerData = {
    text: "Integer id volutpat risus"
}

const footer = (
    '<footer ' + addAttr('class', 'container column') + '>\
    <div ' + addAttr('id', 'footerNav') + '></div>\
        <p>' + footerData.text + '</p>\
    </footer>'
)
